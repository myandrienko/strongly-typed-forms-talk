import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    August 12
    <h1>
      Strongly Typed React Forms, Polymorphic Components, and Other TypeScript
      Bulsh*ttery
    </h1>
  </React.StrictMode>
);
